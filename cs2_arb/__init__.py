"""CS2 portfolio + float-aware arbitrage agent (v1 demo slice).

This package contains the core IP from the PRD: the CSFloat client and the
float-band comparable engine. It runs against fixtures today; supply a real
API key + holdings to run live. Alert-only by design — no trades are placed.
"""

__version__ = "0.1.0"
